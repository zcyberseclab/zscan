cve-id: hikvision-defaultpassword-asdf1234
manual: true
transport: http
rules:
  - method: GET
    path: /ISAPI/Security/userCheck
    headers:
      Authorization: Basic YWRtaW46YXNkZjEyMzQ=
    follow_redirects: false
    expression: |
      response.status == 200 && response.body.bcontains(b"userCheck") && response.body.bcontains(b"statusValue") && response.body.bcontains(b"200")
severity:
  high
type:
  defaultpassword
